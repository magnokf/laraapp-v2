server {
    listen 80;
    listen [::]:80;

    server_name _;

    index index.php index.html;

    # Proxy para o Vite (agora no container PHP)
    location /@vite {
        proxy_pass http://php:5173;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    location ^~ /laravelapp {
        alias /var/www/nested/public;
        try_files $uri $uri/ @laravelapp;

        location ~ \.php$ {
            try_files $uri =404;
            fastcgi_split_path_info ^(.+\.php)(/.+)$;
            fastcgi_pass php:9000;
            fastcgi_index index.php;
            include fastcgi_params;

            # Importante: isso garante que o PHP processe o arquivo correto
            fastcgi_param SCRIPT_FILENAME $request_filename;
            fastcgi_param PATH_INFO $fastcgi_path_info;
        }
    }

    location @laravelapp {
        rewrite /laravelapp/(.*)$ /laravelapp/index.php?/$1 last;
    }

    # Protege arquivos ocultos
    location ~ /\. {
        deny all;
    }
}